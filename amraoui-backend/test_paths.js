const mongoose = require('mongoose');
const { RequestsSchema } = require('./src/app/modules/requests/requests.model');
console.log("Paths:");
// We need to compile the TS code first or just use ts-node
